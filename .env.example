# Required for API routes that need authentication. Use a long random value.
LINKBOX_API_TOKEN=replace-with-a-secret-token

# Express server port.
PORT=3000

# Use production when serving the compiled client/dist SPA from Express.
NODE_ENV=development

# Optional path to the built SPA assets. Relative paths resolve from the repository root.
CLIENT_DIST_DIR=client/dist
