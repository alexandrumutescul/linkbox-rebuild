export default function App() {
  return (
    <main className="app-shell">
      <section className="hero" aria-labelledby="app-title">
        <p className="eyebrow">Linkbox</p>
        <h1 id="app-title">Save and organize links without the clutter.</h1>
        <p className="lede">
          The React SPA is loaded and ready. The Express API is available under <code>/api</code>.
        </p>
      </section>
    </main>
  );
}
